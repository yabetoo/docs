---
title: "Récupérer une intention de paiement"
description: "Récupérez une intention de paiement par son ID."
---

## Endpoint

```bash
GET https://pay.sandbox.yabetoopay.com/v1/payment-intents/{id}   # Sandbox
GET https://pay.api.yabetoopay.com/v1/payment-intents/{id}       # Production
```

## Authentification

Utilisez votre clé secrète dans l'en-tête `Authorization` :

```bash
Authorization: Bearer YOUR_SECRET_KEY
```

## Paramètres de chemin

| Paramètre | Type | Description |
|-----------|------|-------------|
| `id` | `string` | L'ID de l'intention de paiement |

## Exemple de requête

```javascript
const paymentIntentId = "pi_XdLeavDjiiaEx2y5nRz0";

const response = await fetch(
  `https://pay.sandbox.yabetoopay.com/v1/payment-intents/${paymentIntentId}`,
  {
    method: "GET",
    headers: {
      "Content-Type": "application/json",
      Authorization: "Bearer YOUR_SECRET_KEY",
    },
  }
);

const data = await response.json();
```

## Réponse

### 200 OK

```json
{
  "amount": 2000,
  "currency": "xaf",
  "label": "payment_intent",
  "id": "pi_XdLeavDjiiaEx2y5nRz0",
  "status": "succeeded",
  "client_secret": ""
}
```

### Statuts de l'intention de paiement

| Statut | Description |
|--------|-------------|
| `requires_payment_method` | En attente de méthode de paiement |
| `requires_confirmation` | En attente de confirmation |
| `processing` | Paiement en cours de traitement |
| `succeeded` | Paiement réussi |
| `failed` | Paiement échoué |
| `canceled` | Paiement annulé |

### 404 Not Found

```json
{
  "message": "Payment intent not found"
}
```

### 401 Unauthorized

```json
{
  "error": "Unauthorized"
}
```
